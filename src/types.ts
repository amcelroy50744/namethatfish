// If you have any types that are SHARED BY MANY COMPONENTS,put them here
export type TUserScore = {
    correct: number;
    incorrect: number;
  };
export type TFish = {
  name: string;
  url: string;
};
